{
  "name": "mongoose-timestamp-date-unix",
  "description": "Mongoose plugin that adds createdAt and updatedAt auto-assigned date properties (only date) with UNIX format",
  "version": "0.6.1",
  "author": "Kamonwat Sangudsub <kamonwat_s@cmu.ac.th>",
  "keywords": [
    "mongodb",
    "mongoose",
    "plugin",
    "timestamps",
    "createdAt",
    "updatedAt",
    "unix"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/ice5050/mongoose-timestamp-date-unix"
  },
  "devDependencies": {
    "mocha": "~2.5.3",
    "mongoosastic": "^4.0.2",
    "mongoose": "~4.5.3",
    "request": "^2.72.0",
    "should": "~9.0.2"
  },
  "scripts": {
    "test": "mocha -u bdd -R spec -c ./test/*test.js"
  },
  "license": "MIT",
  "engine": {
    "node": ">= 0.6"
  },
  "dependencies": {
    "defaults": "^1.0.3"
  }
}
